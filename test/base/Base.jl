#=
help?> Base
search: Base parse Task else asec haskey falses hash last as

  Base

  The base library of Julia. Base is a module that contains basic
  functionality (the contents of base/). All modules implicitly contain using
  Base, since this is needed in the vast majority of cases.
=#
